let fr_names: string[] = ["Ali", "Sara", "Hiba", "Asad", "Haya"];

for (const name of fr_names) {
  const message = `Hey ${name}!! You're soo beutiful, just looking like a wow!`;
  console.log(message);
}
